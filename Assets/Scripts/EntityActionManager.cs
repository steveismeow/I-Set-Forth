using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EntityActionManager : MonoBehaviour
{
    //Holds base action methods, methods shared by all entities: Move(), Basic Attack()?, etc
}
